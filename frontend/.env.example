VITE_API_BASE_URL=http://localhost:3000/api/v1

# true = dùng mock trong src/api/mocks/ thay vì gọi API thật (mặc định true,
# vì chưa insert data thật / BE chưa xong hết endpoint). Set false khi BE
# sẵn sàng để chuyển hẳn sang gọi thật, không cần sửa code.
VITE_USE_MOCK=true
